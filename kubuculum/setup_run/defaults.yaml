

#
# options not likely to be tuned, similar to ones in most modules
# 

# output directory for this module; always overridden by caller
# dir: 

# namespace for k8s resources; always overridden by caller
# namespace: 

# sub-directory where templates are stored
templates_dir: templates

# for failed runs, cleanup may be required,
# put yaml files in accessible directory
# directory where yaml files are created
yaml_dir: /tmp

# names of template files
role_template: kubuculum.role.j2
rolebinding_template: kubuculum.rolebinding.j2

# names for yaml files derived from templates
role_yaml: kubuculum.role.yaml
rolebinding_yaml: kubuculum.rolebinding.yaml

