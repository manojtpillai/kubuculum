
setup:
  namespace: nm-kubuculum
  basedir: /root/mpillai/runs

run_control:
  benchmark: fio_random

benchmarks:
  fio_random:
    filesize_gb: 2
    numjobs: 2

multirun_control:
  test_list:
    - benchmarks:
       fio_random:
         ninstances: 2
         bs_kb: 4

