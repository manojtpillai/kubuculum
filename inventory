
[hosts]
localhost

[all:vars]

# by default, run output is in a sub-dir in playbook directory
# use run_basedir to change that
run_basedir="/root/mpillai"

# optionally collect stats
stats_enabled=True

# namespace for sysstat pods
# fixed for now; other namespaces not yet working with daemonset
# stats_namespace="kube-system" 

# choose namespace for benchmark 
# there is a chance of inadvertently deleting other pod/pvc, so
# "default" namespace no longer allowed
benchmark_namespace="nm-kubuculum"

# choose benchmark 
# benchmark_name="bench_fioseq"
benchmark_name="bench_fiorand"
# benchmark_name="bench_ycsb"

# fioseq specific options
bench_fioseq_pvcsize_gb=300
bench_fioseq_fsz_gb=16
bench_fioseq_njobs=8

# fiorand specific options
bench_fiorand_pvcsize_gb=300
bench_fiorand_fsz_gb=16
bench_fiorand_njobs=2

# ycsb specific options
bench_ycsb_dbrole="db_mongodb"
bench_ycsb_workload="workloads/workloada"
# bench_ycsb_reccount=100000000
# bench_ycsb_opcount=10000000
bench_ycsb_reccount=1000000
bench_ycsb_opcount=500000
bench_ycsb_run_thrds=8


# pausefor_custombench options
pausefor_custombench_wait_secs=600

